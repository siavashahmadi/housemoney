services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3021:80"
    restart: unless-stopped
    depends_on:
      - backend

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    expose:
      - "8000"
    restart: unless-stopped
